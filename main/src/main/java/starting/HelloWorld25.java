// Java 25 provides implicit wrapper class, simpler main(), but not println()
// Now println() must be imported, from java.lang.IO.

import static java.lang.IO.println;

void main() {
    println("Hello, World!");
}
