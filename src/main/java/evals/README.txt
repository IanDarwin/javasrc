This directory has TWO different implementations.

EvalServlet.* is the servlet implementation.

EvalClient/EvalServer are the RMI implementation
	(which never worked 100%, AFAIK).

Grapher can be used by both.
