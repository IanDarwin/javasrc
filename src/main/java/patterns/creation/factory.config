# The Factory code is from another example; the names
# renderer and provider are just there to show that
# you can create an aribrary number of simple POJOs
# as long as all the classes have no-argument constructors
renderer=java.lang.String
provider=java.util.Date