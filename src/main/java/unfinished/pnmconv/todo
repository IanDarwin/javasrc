AllGfxTypes needs a for loop to check for any valid type.

Main window to have a textfield with From filename, and Browse.
A PullDown of all To types. Use first extention for type.

Types:
	final int PGM=1, PGM=2, PPM=3
Three static icons: black circle, gray circle, and RGB circle.
	switch (type)
		PGM: icon=bwIcon;
		PGM: icon=iconGray;
		PPM: icon=iconColor;

