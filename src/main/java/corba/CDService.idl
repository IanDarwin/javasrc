/* The CD Lookup */
module CDService {
	struct CD {
		long cdNumber;
		string title;
	};

	struct Track {
		string title;
		short runSeconds;
	};

	typedef sequence <Track> Tracks;

	interface CDList {
		CD getCD(in long cdNum);
		Tracks  getTracks();
	};
};
